# Test Environment Variables
NODE_ENV=test
PORT=3001

# Database (use test database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_gateway_test
DB_USER=postgres
DB_PASSWORD=test

# Redis (use test instance)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Security (test keys)
JWT_SECRET=test-jwt-secret-key
ENCRYPTION_KEY=test-encryption-key-32-characters
WEBHOOK_SECRET=test-webhook-secret

# URLs
WEBHOOK_BASE_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000

# Public API endpoints for testing (no credentials needed)
BITCOIN_RPC_ENDPOINTS=https://blockstream.info/testnet/api
ETHEREUM_RPC_ENDPOINTS=https://ethereum-rpc.publicnode.com
PRICE_API_ENDPOINTS=https://api.coingecko.com/api/v3

# Logging
LOG_LEVEL=error
LOG_DIR=./logs/test